import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div style={{ display: "flex", minHeight: "100vh", width: "100%" }}>
      <p style={{ textAlign: "center" }}>Sample Prisma Issue</p>
    </div>
  );
};

export default Home;
